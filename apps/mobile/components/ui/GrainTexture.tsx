/**
 * Grain texture placeholder.
 * SVG-based grain requires react-native-svg with filter support
 * which isn't available in Expo Go. The warm cream background
 * (#FBF5EC) achieves the same "not sterile" feel on its own.
 * Replace with a real implementation when using a dev build.
 */
export function GrainTexture() {
  return null;
}
